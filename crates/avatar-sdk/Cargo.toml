[package]
name = "avatar-sdk"
version = "0.1.0"
edition = "2021"
description = "Avatar state and action definitions for Lua/Rust scripting bridge"

[dependencies]
# No external dependencies — pure data structures

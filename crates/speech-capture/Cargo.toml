[package]
name = "speech-capture"
version = "0.1.0"
edition = "2021"
description = "Voice activity detection from microphone with speech segmentation"

[dependencies]
audio-capture = { path = "../audio-capture" }
ten-vad = { path = "../ten-vad" }
log = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
env_logger = { workspace = true }

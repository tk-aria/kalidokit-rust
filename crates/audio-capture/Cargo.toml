[package]
name = "audio-capture"
version = "0.1.0"
edition = "2021"
description = "Cross-platform microphone audio capture with 16kHz mono output"

[dependencies]
cpal = "0.17"
log = { workspace = true }
thiserror = { workspace = true }
